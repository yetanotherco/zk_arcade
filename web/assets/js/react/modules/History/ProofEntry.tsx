import React from "react";
import { ProofSubmission } from "../../types/aligned";

type Props = {
	proofs: ProofSubmission[];
};

export const ProofEntry = ({ proofs }: Props) => {
	return <div></div>;
};
