import React from "react";
import { Address } from "../../types/blockchain";

type Props = {
	leaderboard_address?: Address;
	proofs: string;
};

export const ProofHistory = ({ proofs, leaderboard_address }: Props) => {
	return (
		<>
			<div></div>
		</>
	);
};
